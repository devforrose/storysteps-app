import ContentLibrary from '@/components/Reader/ContentLibrary';

export const metadata = {
  title: 'Reading Library - StorySteps',
  description: 'Explore comprehensible input stories and articles for language learning',
};

export default function ReaderPage() {
  return <ContentLibrary />;
}
